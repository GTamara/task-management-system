{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for tTask Management System",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000 --delay 1000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
